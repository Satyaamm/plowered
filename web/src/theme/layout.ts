// Spacing / sizing tokens. Pure values — no Fluent UI imports. Safe to
// reach for from Server Components without pulling the client runtime
// into the server bundle.

export const layout = {
  pageMaxWidth: "1024px",
  pagePaddingY: "32px",
  pagePaddingX: "24px",
  cardGap: "12px",
};
