{{ include "plowered.fullname" . }} has been installed.

To check the deployment:
  kubectl --namespace {{ .Release.Namespace }} rollout status deployment/{{ include "plowered.fullname" . }}

{{- if .Values.ingress.enabled }}

The HTTP API is reachable at:
  {{- range .Values.ingress.hosts }}
  https://{{ .host }}/
  {{- end }}
{{- else }}

To reach the HTTP API locally:
  kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ include "plowered.fullname" . }} 8080:{{ .Values.service.httpPort }}
  curl -H "Authorization: Bearer <token>" http://localhost:8080/v1/assets
{{- end }}

To reach the gRPC port locally:
  kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ include "plowered.fullname" . }} 9090:{{ .Values.service.grpcPort }}
