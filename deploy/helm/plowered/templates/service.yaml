apiVersion: v1
kind: Service
metadata:
  name: {{ include "plowered.fullname" . }}
  labels: {{- include "plowered.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector: {{- include "plowered.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.httpPort }}
      targetPort: http
      protocol: TCP
    - name: grpc
      port: {{ .Values.service.grpcPort }}
      targetPort: grpc
      protocol: TCP
